declare type CurriedReturn = (value: number) => number;
export declare function clamp(min: number, max: number): CurriedReturn;
export declare function clamp(min: number, max: number, value: number): number;
export {};
