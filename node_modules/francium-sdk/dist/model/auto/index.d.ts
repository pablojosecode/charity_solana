import { Connection, PublicKey } from '@solana/web3.js';
import { Idl, Program } from '@project-serum/anchor';
export default class FranciumAutoVaults {
    configs: any;
    programs: {};
    connection: Connection;
    allPoolList: any[];
    constructor(config: {
        connection: Connection;
    });
    getProgram(type?: string): Program<Idl>;
    getConfig(pair: string, type?: string): any;
    getUserPositionsAll(userPublicKey: PublicKey | string): Promise<{
        orca: any[];
        raydium: {
            publicKey: PublicKey;
            data: any;
        }[];
    }>;
    getFarmPoolsInfo(): Promise<any[]>;
    getUserFarmPosition(userPublicKey?: PublicKey): Promise<{
        pool: any[];
        user: any[];
    }>;
    formatUserAutoPosition(poolInfo: any, userInfo: any, config: {
        token0: string;
        token1: string;
        lpDecimals: number;
        depositToken?: string;
    }): {
        pool: {};
        user: {};
    };
    getUserPositionsByProgram(programType: string, userPublicKey: PublicKey): Promise<{
        publicKey: PublicKey;
        data: any;
    }[]>;
}
