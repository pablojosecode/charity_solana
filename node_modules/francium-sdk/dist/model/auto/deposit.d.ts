import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import FranciumAutoVaults from ".";
import { BN } from "@project-serum/anchor";
export default function depositAuto(connection: Connection, pair: string, lyfType: string, userPublicKey: PublicKey, hub: FranciumAutoVaults, configs: {
    amount0: BN;
    amount1?: BN;
    userPositionPublicKey?: PublicKey;
}): Promise<Transaction[]>;
