import { BN } from "@project-serum/anchor";
import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import FranciumAutoVaults from ".";
export default function withdrawAuto(connection: Connection, pair: string, lyfType: string, userPublicKey: PublicKey, hub: FranciumAutoVaults, configs: {
    shareAmount: BN;
    userPositionPublicKey: PublicKey;
}): Promise<Transaction[]>;
