import { Connection, Keypair, PublicKey, Transaction } from "@solana/web3.js";
export declare function deposit(connection: Connection, amount: number, pool: string, userPublicKey: PublicKey, configs?: {
    noRewards?: boolean;
    mintStSol?: boolean;
}): Promise<{
    trx: Transaction;
    signers: Keypair[];
}>;
export declare function lendWithReward(connection: Connection, amount: number, pool: string, userPublicKey: PublicKey, trx?: Transaction, mintStSol?: boolean): Promise<Transaction>;
