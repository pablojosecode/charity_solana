import { Connection, Keypair, PublicKey, Transaction } from "@solana/web3.js";
export declare function withdraw(connection: Connection, rewardAmount: number, tokenAmount: number, pool: string, userPublicKey: PublicKey, configs?: {
    noRewards?: boolean;
}): Promise<{
    trx: Transaction;
    signers: Keypair[];
}>;
export declare function unstakeReward(connection: Connection, amount: number, pool: string, userPublicKey: PublicKey, trx?: Transaction, configs?: {
    userCollateralTokenAccount?: string | PublicKey;
}): Promise<Transaction>;
