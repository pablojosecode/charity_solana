import { Connection, PublicKey, TransactionInstruction } from '@solana/web3.js';
import * as BN from 'bn.js';
export declare enum INSTRUCTION {
    STAKE = 1,
    UNSTAKE = 2
}
export declare const MAINNET_PROGRAM_ADDRESSES: {
    solidoProgramId: PublicKey;
    solidoInstanceId: PublicKey;
    stSolMintAddress: PublicKey;
    reserveAccount: PublicKey;
    mintAuthority: PublicKey;
};
export default function mintStSol(userPublicKey: PublicKey, userTokenAccount: PublicKey, amount: BN): TransactionInstruction;
export declare function addReferral(userPublicKey: PublicKey): TransactionInstruction;
export declare function getStSolExchangeRate(connection: Connection): Promise<number>;
