import { Connection } from "@solana/web3.js";
import { BaseLPInfo, FormatLPInfo } from "./types";
export declare function getRaydiumLPInfo(connection: Connection): Promise<{
    [pool: string]: BaseLPInfo;
}>;
export declare function getRaydiumLPPrice(connection: Connection, priceList: {
    [symbol: string]: number;
}): Promise<{
    [pool: string]: FormatLPInfo;
}>;
