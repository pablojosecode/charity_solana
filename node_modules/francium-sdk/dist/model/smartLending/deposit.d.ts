import { BN } from '@project-serum/anchor';
import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import { FranciumSmartLending } from ".";
export declare function smartLendingDeposit(connection: Connection, amount: BN, pool: string, lendHub: FranciumSmartLending, userPublicKey: PublicKey, configs?: {
    userInfoAccount?: PublicKey;
    lockupTime: number;
}): Promise<{
    trx: Transaction;
    signers: any;
}>;
