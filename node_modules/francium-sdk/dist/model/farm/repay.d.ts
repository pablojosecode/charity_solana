import { BN } from "@project-serum/anchor";
import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import { FranciumFarm } from ".";
export declare function buildRepayTransactions(connection: Connection, pair: string, lyfType: string, userPublicKey: PublicKey, farm: FranciumFarm, configs: {
    amount0: BN;
    amount1: BN;
    currentUserInfoAccount: PublicKey;
}): Promise<Transaction>;
