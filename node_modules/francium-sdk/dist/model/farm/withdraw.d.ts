import { Connection, PublicKey, Transaction } from "@solana/web3.js";
import { FranciumFarm } from '.';
import BigNumber from "bignumber.js";
export default function buildWithdrawTransactions(connection: Connection, pair: string, lyfType: string, userPublicKey: PublicKey, farm: FranciumFarm, configs: {
    lpShares: BigNumber;
    withdrawType: number;
    currentUserInfoAccount: PublicKey;
}): Promise<{
    closedAccount: any;
    trxs: Transaction[];
}>;
