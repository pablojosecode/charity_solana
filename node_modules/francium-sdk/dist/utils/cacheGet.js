"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const axios_1 = require("axios");
const promiseCache = {};
function cacheGet(url, config, useCache = true) {
    if (!useCache) {
        return axios_1.default.get(url, config);
    }
    if (promiseCache[url]) {
        return promiseCache[url];
    }
    const req = axios_1.default.get(url, config);
    promiseCache[url] = req;
    return req
        .then(res => {
        promiseCache[url] = null;
        return Promise.resolve(res);
    })
        .catch((err) => {
        promiseCache[url] = null;
        return Promise.reject(err);
    });
}
exports.default = cacheGet;
