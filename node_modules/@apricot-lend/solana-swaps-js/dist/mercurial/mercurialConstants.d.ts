import { PublicKey, TransactionInstruction } from "@solana/web3.js";
import { Market, PairMarket, Swapper, TokenID } from "../types";
import { Parser } from "../utils/Parser";
export declare const MERCURIAL_SWAP_PROGRAM: PublicKey;
export declare class MercurialMarket extends Market implements Swapper, PairMarket {
    name: string;
    tokenIdA: TokenID;
    tokenIdB: TokenID;
    swap: PublicKey;
    swapAuthority: PublicKey;
    vaultA: PublicKey;
    vaultB: PublicKey;
    vaultC: PublicKey;
    constructor(name: string, tokenIdA: TokenID, tokenIdB: TokenID, swap: PublicKey, swapAuthority: PublicKey, vaultA: PublicKey, vaultB: PublicKey, vaultC: PublicKey);
    getSwapper(): Swapper;
    INST_LAYOUT: Parser;
    createSwapInstructions(_fromToken: TokenID, fromAmount: number, fromTokenAccount: PublicKey, _toToken: TokenID, minToAmount: number, toTokenAccount: PublicKey, tradeOwner: PublicKey): Promise<TransactionInstruction[]>;
}
export declare const MERCURIAL_USTv1_USDC_MARKET: MercurialMarket;
