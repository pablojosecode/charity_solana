import { Commitment, Connection, ConnectionConfig } from '@solana/web3.js';
import { RPC } from '../types';
export declare class RpcNode {
    readonly rpc: RPC;
    readonly url: string;
    private _connection;
    constructor(rpc: RPC, url: string);
    getConnection(commitmentOrConfig?: Commitment | ConnectionConfig): Connection;
}
export declare const rpcNodes: Record<RPC, RpcNode>;
export declare const getRPCConnection: (rpcish?: string | undefined, commitmentOrConfig?: Commitment | ConnectionConfig | undefined) => Connection;
