import { Numerical } from "./Numerical";
import { Decimal } from "./Decimal";
import Big, { BigSource } from "big.js";
export declare abstract class WrappedDecimal<D extends Decimal<D>, W extends WrappedDecimal<D, W>> extends Numerical<W> {
    private readonly wrapped;
    protected constructor(wrapped: D);
    getRaw(): Big;
    getWrapped(): D;
    replaceWithValue(value: BigSource): W;
    protected abstract wrap(value: D): W;
}
//# sourceMappingURL=WrappedDecimal.d.ts.map