import { WrappedLamport } from "./WrappedLamport";
export class Token extends WrappedLamport {
    constructor(mintId, lamport) {
        super(lamport);
        this.mintId = mintId;
    }
    isNative() {
        return this.getMintId().isNative();
    }
    getMintId() {
        return this.mintId;
    }
    isCompatibleWith(that) {
        return this.mintId.equals(that.mintId);
    }
}
//# sourceMappingURL=Token.js.map