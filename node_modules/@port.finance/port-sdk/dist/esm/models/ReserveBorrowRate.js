import { Ratio } from "./basic";
export class ReserveBorrowRate extends Ratio {
    constructor(mintId, pct) {
        super(pct);
        this.mintId = mintId;
    }
    static na(mintId) {
        return new ReserveBorrowRate(mintId);
    }
    getMintId() {
        return this.mintId;
    }
    isCompatibleWith(that) {
        return this.mintId.equals(that.mintId);
    }
}
//# sourceMappingURL=ReserveBorrowRate.js.map