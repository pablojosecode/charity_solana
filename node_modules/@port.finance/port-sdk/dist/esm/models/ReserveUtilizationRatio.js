import { Ratio } from "./basic";
export class ReserveUtilizationRatio extends Ratio {
    constructor(mintId, pct) {
        super(pct);
        this.mintId = mintId;
    }
    static na(mintId) {
        return new ReserveUtilizationRatio(mintId);
    }
    getMintId() {
        return this.mintId;
    }
    isCompatibleWith(that) {
        return this.mintId.equals(that.mintId);
    }
}
//# sourceMappingURL=ReserveUtilizationRatio.js.map