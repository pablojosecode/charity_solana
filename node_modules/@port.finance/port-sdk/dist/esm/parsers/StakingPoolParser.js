import { StakingPoolLayout, } from "../structs/StakingPoolData";
export const stakingPoolParser = (raw) => {
    const pubkey = raw.pubkey;
    const buffer = Buffer.from(raw.account.data);
    const data = StakingPoolLayout.decode(buffer);
    return { pubkey, data };
};
//# sourceMappingURL=StakingPoolParser.js.map