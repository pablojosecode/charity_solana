import { ReserveLayout } from "../structs/ReserveData";
export const ReserveParser = (raw) => {
    const pubkey = raw.pubkey;
    const buffer = Buffer.from(raw.account.data);
    const data = ReserveLayout.decode(buffer);
    if (data.lastUpdate.slot.isZero()) {
        return;
    }
    return { pubkey, data };
};
//# sourceMappingURL=ReserveParser.js.map