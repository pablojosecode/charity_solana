import { Percentage } from "./Percentage";
import Big, { Comparison } from "big.js";
import { Comparable } from "./Comparable";
export declare abstract class Ratio<R extends Ratio<R>> extends Comparable<R> {
    private readonly pct?;
    protected constructor(pct?: Percentage);
    isTrivial(): boolean;
    isPresent(): boolean;
    isPositive(): boolean;
    isNegative(): boolean;
    getUnchecked(): Big;
    getPct(): Percentage | undefined;
    print(): string;
    compare(that: R): Comparison;
    toString(): string;
}
//# sourceMappingURL=Ratio.d.ts.map