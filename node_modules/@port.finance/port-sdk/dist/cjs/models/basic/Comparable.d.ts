import { Comparison } from "big.js";
export declare abstract class Comparable<C extends Comparable<C>> {
    eq(that?: C): boolean;
    lt(that?: C): boolean;
    lte(that?: C): boolean;
    gt(that?: C): boolean;
    gte(that?: C): boolean;
    protected abstract compare(that: C): Comparison;
    protected abstract isCompatibleWith(that: C): boolean;
    protected checkCompatible(that: C): void;
    private checkAndCompare;
}
//# sourceMappingURL=Comparable.d.ts.map