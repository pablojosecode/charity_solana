"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateReserveInstruction = void 0;
const web3_js_1 = require("@solana/web3.js");
const spl_token_1 = require("@solana/spl-token");
const BufferLayout = __importStar(require("@solana/buffer-layout"));
const instruction_1 = require("./instruction");
const Instructions_1 = require("../../utils/Instructions");
const ReserveData_1 = require("../../structs/ReserveData");
const constants_1 = require("../../constants");
// Update configuration for an existing market reserve.
//
// Accounts expected by this instruction:
//
//   0. `[writable]` Reserve account
//   1. `[]` Lending market account.
//   2. `[]` Derived lending market authority.
//   3. `[signer]` Lending market owner.
//   4. `[]` Rent sysvar.
//   5. `[]` Token program id.
const updateReserveInstruction = (config, reservePubkey, // 0
lendingMarketPubkey, // 1
lendingMarketAuthorityPubkey, // 2
lendingMarketOwnerPubkey, // 3
lendingProgramId = constants_1.PORT_LENDING) => {
    const dataLayout = BufferLayout.struct([
        BufferLayout.u8("instruction"),
        // eslint-disable-next-line new-cap
        (0, ReserveData_1.ReserveConfigLayout)("config"),
    ]);
    const data = Buffer.alloc(dataLayout.span);
    dataLayout.encode({
        instruction: instruction_1.LendingInstruction.UpdateReserve,
        config,
    }, data);
    const keys = [
        (0, Instructions_1.getAccess)(reservePubkey, Instructions_1.AccessType.WRITE),
        (0, Instructions_1.getAccess)(lendingMarketPubkey, Instructions_1.AccessType.READ),
        (0, Instructions_1.getAccess)(lendingMarketAuthorityPubkey, Instructions_1.AccessType.READ),
        (0, Instructions_1.getAccess)(lendingMarketOwnerPubkey, Instructions_1.AccessType.SIGNER),
        (0, Instructions_1.getAccess)(web3_js_1.SYSVAR_RENT_PUBKEY, Instructions_1.AccessType.READ),
        (0, Instructions_1.getAccess)(spl_token_1.TOKEN_PROGRAM_ID, Instructions_1.AccessType.READ),
    ];
    return new web3_js_1.TransactionInstruction({
        keys,
        programId: lendingProgramId,
        data,
    });
};
exports.updateReserveInstruction = updateReserveInstruction;
//# sourceMappingURL=updateReserve.js.map